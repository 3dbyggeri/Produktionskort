<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Produktionskort" %></title>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <%= stylesheet_link_tag 'reset', 'application', 'jNice', 'formtastic', 'formtastic_changes', :cache => true %>
  <%= stylesheet_link_tag 'print_work_process', :media => 'print' %>
  <!--[if IE 6]>
  <%= stylesheet_link_tag 'ie6', :media => 'screen', :cached => true %>
  <![endif]-->
  <!--[if IE 7]>
  <%= stylesheet_link_tag 'ie7', :media => 'screen', :cached => true %>
  <![endif]-->
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <%= yield :head %>
</head>
<body>
  <div id="wrapper">
    <h1><a href="/"><span>Produktionskort</span></a></h1>
    <div id="active_project_name"><%= link_to(@project.name, edit_project_path(@project)) if @project && !@project.new_record? %></div>

    <ul id="mainNav">
      <li><%= link_to 'Produktionskort', root_path, :class => (current_page?(about_path) ? nil : 'active') %></li>
      <li><%= link_to 'Om denne applikation', about_path, :class => (current_page?(about_path) ? 'active' : nil) %></li>
      <li class="project">
        <span>
          <%= form_tag switch_projects_path, :method => :post do |f| %>
            <label for="id">Skift byggesag:</label>
            <select name="id" id="switch_project">
              <option value=""></option>
              <%= options_for_select Project.all.map { |p| [p.name, p.id] } %>
              <option value="new">Opret ny...</option>
            </select>
          <% end %>
        </span>
      </li>
    </ul>

    <div id="containerHolder">
      <div id="container">
        <h2><%= yield :title %></h2>
        <%= yield :quicklinks %>

        <div id="sidebar">
          <%= yield :sidebar %>
        </div>

        <div id="main">
          <% flash.each do |name, msg| %>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
          <% end %>
          <%= yield %>
        </div>
        <div class="clear"></div>
      </div>
    </div>
    <p id="footer">
      Udviklet i samarbejde med
      <a href="http://www.3dbyggeri.dk">3dbyggeri danmark</a>,
      <a href="http://www.codegroup.dk">CodeGroup</a>,
      <a href="http://www.eg.dk">EG</a>,
      <a href="http://www.jakon.dk">JAKON</a> og
      <a href="http://www.linkslabs.com">LinKS Advice</a>
    </p>
  </div>
</body>
</html>
